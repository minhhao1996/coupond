@extends('layouts.app')
@section('content')
<section class="mx-auto max-w-7xl px-5 pt-10 lg:px-8"><div class="rounded-[2rem] bg-emerald-950 p-8 text-white md:p-10"><div class="text-xs font-black uppercase tracking-[.2em] text-emerald-300">EDITORIAL</div><h1 class="mt-2 text-4xl font-black tracking-tight md:text-5xl">Reviews & buying guides</h1><p class="mt-3 max-w-2xl text-emerald-100/70">Practical coverage of products and brands, written to help shoppers compare before they buy.</p></div></section>
@if($featured)<section class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><a href="{{ route('reviews.show',$featured) }}" class="grid overflow-hidden rounded-[2rem] border border-stone-200 bg-white shadow-sm lg:grid-cols-[1.2fr_.8fr]">@if($featured->image_url)<img src="{{ $featured->image_url }}" @if($featured->image_srcset)srcset="{{ $featured->image_srcset }}" sizes="(min-width: 1024px) 720px, 100vw" @endif alt="{{ $featured->title }}" loading="lazy" width="720" height="400" class="h-full min-h-72 w-full object-cover">@else<div class="min-h-72 bg-gradient-to-br from-stone-200 via-emerald-50 to-orange-100"></div>@endif<div class="flex flex-col justify-center p-8"><div class="eyebrow">FEATURED {{ strtoupper($featured->type) }}</div><h2 class="mt-3 text-3xl font-black tracking-tight">{{ $featured->title }}</h2><p class="mt-4 leading-7 text-stone-600">{{ $featured->excerpt }}</p><div class="mt-5 text-sm font-black text-emerald-800">Read article →</div></div></a></section>@endif
<section class="mx-auto max-w-7xl px-5 pb-10 lg:px-8"><div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">@foreach($reviews as $review)<a href="{{ route('reviews.show',$review) }}" class="group overflow-hidden rounded-3xl border border-stone-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg">@if($review->image_url)<img src="{{ $review->image_url }}" @if($review->image_srcset)srcset="{{ $review->image_srcset }}" sizes="(min-width: 1024px) 400px, (min-width: 640px) 50vw, 100vw" @endif alt="{{ $review->title }}" loading="lazy" width="720" height="400" class="h-48 w-full object-cover">@else<div class="h-48 bg-gradient-to-br from-stone-200 via-white to-emerald-100"></div>@endif<div class="p-5"><div class="flex items-center gap-2 text-[10px] font-black uppercase tracking-[.14em] text-emerald-700"><span>{{ $review->category?->name ?? 'Shopping' }}</span><span>•</span><span>{{ $review->type }}</span></div><h2 class="mt-2 text-xl font-black leading-snug">{{ $review->title }}</h2><p class="mt-3 line-clamp-3 text-sm leading-6 text-stone-600">{{ $review->excerpt }}</p><div class="mt-4 text-xs text-stone-500">{{ optional($review->published_at)->format('M j, Y') }}</div></div></a>@endforeach</div><div class="mt-8">{{ $reviews->links() }}</div></section>
@endsection
